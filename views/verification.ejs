<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SurgeView Marketing: Verify Your Email</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link href="/public/style.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
  <script>
        const shouldRedirect = <%= isValid %>;
        if (shouldRedirect) {
          setTimeout(() => {
              window.location.href = '/payment';
          }, 1500); // Delay in milliseconds
        }

  </script>
</head>
<body>
<%- include('partials/header_nolinks') %>
<section class="verification-section py-5">
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-md-6"> <!-- Change the width here -->
        <h2 class="text-center">Verify Your Email</h2>
        <p class="text-center">Please enter the verification code sent to your email: <%- email %>.</p>
        <p> Your email is required to access the dashboard, which is why we require it to be verified.</p>
        <% if(isInvalid) { %>
          <div class="alert alert-danger" role="alert">
              <i class="fas fa-exclamation-triangle"></i> Invalid code. Please try again.
          </div>
        <%} else if(isValid) { %>
          <div class="alert alert-success" role="alert">
            <i class="fas fa-check-circle"></i> Email Verified. Redirecting...
          </div>
        <% } %>
          <form id="verification-form" class="signup-form mt-4" method="post" action="/verification">
            <div class="form-group">
              <label for="verification-code">Verification Code</label>
              <input type="text" class="form-control" id="verification-code" name="verificationcode" placeholder="Enter your verification code" required>
            </div>
  
            <div class="text-center verify-btn-container">
              <button type="submit" class="btn btn-primary">Verify Code</button>
            </div>
  
            <p class="mt-3 text-center">
              Didn't receive a code? <a href="#" class="text-primary">Resend Code</a>
            </p>
          </form>
        </div>
      </div>
  </div>
</section>
<%- include('partials/footer') %>
</body>
</html>

